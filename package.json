{
  "name": "dg",
  "private": true,
  "type": "module",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently -n BACK,FRONT -c blue,magenta \"bun -F backend dev\" \"bun -F frontend dev\"",
    "db:seed": "bun -F backend db:seed",
    "build": "bun run check && bun -F backend run build",
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "@biomejs/biome": "2.2.4",
    "typescript": "~5.9.3"
  }
}